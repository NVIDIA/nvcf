# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

{{- if (include "nvcaop.clusterValidatorEnabled" .) }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nvcaop.fullname" . }}-cluster-validator
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nvcaop.labels" . | nindent 4 }}
    app.kubernetes.io/component: validation
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "nvcaop.fullname" . }}-cluster-validator
  labels:
    {{- include "nvcaop.labels" . | nindent 4 }}
    app.kubernetes.io/component: validation
rules:
  - apiGroups: [""]
    resources:
      - nodes
      - services
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list", "create", "delete"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "create", "delete", "watch"]
  - apiGroups: [""]
    resources: ["configmaps"]
    # get: read the network-checks config ConfigMap (its name is overridable
    # via VALIDATOR_CONFIG_NAME, so this cannot be scoped by resourceName).
    # create: write the cluster-validator-summary ConfigMap on the first run
    # (create cannot be restricted by resourceName in Kubernetes RBAC).
    verbs: ["get", "create"]
  - apiGroups: [""]
    resources: ["configmaps"]
    # update only ever targets the cluster-validator-summary ConfigMap the
    # NVCA agent watches for the metrics pipeline, so scope it by name to
    # limit blast radius if the validator ServiceAccount is compromised.
    resourceNames: ["cluster-validator-summary"]
    verbs: ["update"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets"]
    # statefulsets: the Tier-2 quorum and placement check. Without it every
    # control-plane namespace 403s and the critical row is permanently unknown.
    verbs: ["get", "list"]
  - apiGroups: ["apps"]
    resources: ["daemonsets"]
    # create/delete: the node-to-node overlay probe runs a short-lived
    # DaemonSet in a per-run namespace and deletes it in the same run.
    verbs: ["get", "list", "create", "delete"]
  - apiGroups: ["admissionregistration.k8s.io"]
    resources:
      - mutatingwebhookconfigurations
      - validatingwebhookconfigurations
    verbs: ["get", "list"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["networkpolicies"]
    verbs: ["get", "list", "create", "update", "delete"]
  - apiGroups: ["storage.k8s.io"]
    # storageclasses: the default-StorageClass check. Without it the critical
    # row is permanently unknown on every run.
    resources: ["csidrivers", "storageclasses"]
    verbs: ["get", "list"]
  - apiGroups: ["nvidia.com"]
    resources: ["clusterpolicies"]
    verbs: ["get", "list"]
  - nonResourceURLs: ["/readyz", "/version", "/healthz"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "nvcaop.fullname" . }}-cluster-validator
  labels:
    {{- include "nvcaop.labels" . | nindent 4 }}
    app.kubernetes.io/component: validation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "nvcaop.fullname" . }}-cluster-validator
subjects:
  - kind: ServiceAccount
    name: {{ include "nvcaop.fullname" . }}-cluster-validator
    namespace: {{ .Release.Namespace }}
{{- end }}
